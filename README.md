# Hughes Room Views Repository Page Systems

This branch contains the unpublished Arctic Preferred Black Hole Museum page system. The historical repository runtime and layout laboratories remain preserved as test evidence.

## Black Hole Museum

Current laboratory route: `/repository-page-lab/`

Current presentation release: `0.1.0-black-hole-lab.8`

The implementation separates verified scientific content/media, the repository runtime, proven viewport breakout, Maximum-Shelf subject presentation, environmental enhancements, wide-canvas/performance rules, the simulated YouTube Media Center, route-scoped Amadeus compatibility, and release evidence.

The twelve approved scientific assets have been acquired. The lean `.2` science/media foundation was previously verified. Release `.3` preserves the proven Edublogs viewport correction. Release `.7` is the prior Maximum-Shelf visual checkpoint. Release `.8` adds the current wide desktop composition, larger typography, word-wrap fixes, current-plus-one chamber activation budget, redundant-control cleanup, and lazy two-video Media Center while reusing the verified scientific foundation.

## Source layout

The Black Hole Museum source lives under `apps/black-hole-museum/`. Presentation source is separated into:

- `page.css`
- `viewport-breakout.css`
- `maximum-shelf.css`
- `maximum-shelf-enhancements.css`
- `maximum-shelf-finishing.css`
- `maximum-shelf-wide-performance.css`
- `maximum-shelf-media-center.css`
- `maximum-shelf-runtime.js`
- `theme-amadeus.css`

The historical laboratories remain under `test/repo-runtime-lab/` and `test/repo-layout-lab/`. They are evidence sources and are not modified by this page system.

Desktop presentation treats the viewport as the museum floor plan. Two- and three-lane layouts are preferred when the content supports them; the single-column edition is reserved for mobile.

The runtime keeps the current chamber plus one chamber ahead warm and makes distant chambers dormant. The simulated Media Center uses two privacy-enhanced YouTube embeds with standard controls and no autoplay; iframe players are created only near the viewport.

## Tests

The feature branch includes viewport and presentation contract tests plus `.github/workflows/black-hole-presentation-ci.yml` as a lightweight regression gate.

## Current handoff

The current Edublogs block is `docs/deployment/black-hole-museum-edublogs-block.html`. It preserves the semantic native fallback and points to the immutable `.8` presentation release manifest.

No production classroom data is connected to this laboratory. The Media Center is visibly marked as a simulated selection rather than a record of Ms. Hughes approval.
