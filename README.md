# Hughes Room Views Black Hole Museum CDN Delivery

This orphan branch contains only the immutable browser-delivery payload for $NewTag.
The source implementation remains on $FeatureBranch.
The previous $OldTag is preserved and was not moved.
